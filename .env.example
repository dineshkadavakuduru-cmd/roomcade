NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_RTDB_URL=
NEXT_PUBLIC_FIREBASE_APP_ID=
# Fill these to enable cross-device multiplayer (Firestore room metadata +
# Realtime Database live state). Left empty, Roomcade runs on a local
# same-browser backend so two tabs can still complete the full flow.
